During the 2026-09-17 triage sweep, #402's own tracker `classify:` line read
`could-not-classify (spawn timed out after 45s)` -- the board's automated
classifier failed to render a verdict on this issue at all, rather than
producing a wrong one.

Not chased further by this sweep: whether the timeout is a property of #402's
own body (something in it made classification slow) or a transient
infrastructure hiccup unrelated to the issue's content. Worth a look if a
second issue shows the same `could-not-classify (spawn timed out ...)` shape,
which would point at the classifier itself rather than at #402.
